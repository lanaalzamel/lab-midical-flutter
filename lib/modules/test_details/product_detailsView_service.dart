import 'package:flutter/material.dart';

class ProductDetailsService extends StatelessWidget {
  const ProductDetailsService({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
